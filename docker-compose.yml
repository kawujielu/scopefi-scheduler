services:
  scheduler:
    build: .
    container_name: scopefi-scheduler
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./scopefi/.env:/app/scopefi/.env:ro
      - ./logs:/app/logs
